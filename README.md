# Milestonevault
Escrowed grant milestones on GenLayer — locks GEN against a GitHub-verifiable criterion (star count, PR merge, release tag) and releases it only when the vault's own independent fetch confirms it.
